// console.log(window.screen.width);
if (window.screen.width <= 800) {
    // console.log("hello small device");

    // * Constant Variables
    const nav = document.querySelector("nav");

    // * Event Listeners
    // nav.addEventListener("focusout", toggelNavUl)

    // * Functions
}